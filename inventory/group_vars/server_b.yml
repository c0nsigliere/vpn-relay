---
# Variables specific to Server B (wg_cascade group).
# Uncomment and set values here to override group-wide defaults for Server B only.

# Example: pin the WAN interface if auto-detection is unreliable
# wan_if: "eth0"
